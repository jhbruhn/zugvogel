/// Generic PocketBase repository layer.
///
/// Filters are built, never concatenated: a raw filter string must stay a
/// compile error at the call site — see eiermann-d2a.2.
library;

// Exports land here as eiermann-d2a.2 moves the repository across.
